# Server
PORT=4000
HOST=0.0.0.0
CLIENT_ORIGIN=http://localhost:3000

# Database (SQLite path; create data/ dir or set absolute path)
DATABASE_PATH=./data/sqlite.db

# Better Auth (required: 32+ char secret, base URL of this server)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:4000
