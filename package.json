{
  "name": "nodejs-multiplayer-game-starter",
  "type": "module",
  "scripts": {
    "dev": "node builderman.js dev",
    "build": "node builderman.js build",
    "db:generate": "pnpm --filter server db:generate",
    "db:migrate": "pnpm --filter server db:migrate",
    "db:seed": "pnpm --filter server db:seed"
  },
  "packageManager": "pnpm@10.28.0",
  "dependencies": {
    "better-auth": "^1.4.0"
  },
  "devDependencies": {
    "@builderman/resolvers-pnpm": "^1.0.0",
    "builderman": "^1.6.0"
  }
}
